interface:
  display_name: "Relationship Memory Companion"
  short_description: "Build a private relationship-memory companion bundle"
  default_prompt: "Export or prepare my authorized chat logs, apply the companion privacy layer by default, verify export health, distill the voice into emotional-memory, relationship, scenario, and session artifacts, then evaluate readiness for private companion chat without visible router labels."
